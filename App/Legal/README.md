# Legal

Licences for C2UI and for every third-party component the editor ships.

Licences that belong to an SDK placed in `Core/dev-kit` stay with that SDK, since
its files are not part of this repository.
