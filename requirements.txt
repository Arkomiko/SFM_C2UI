# The editor's user interface and viewport. The engine in Core/ and the test
# suite need nothing beyond the standard library.
#
#   python -m venv .venv
#   .venv/Scripts/python.exe -m pip install -r requirements.txt
#   .venv/Scripts/python.exe c2ui.py
PySide6>=6.6
PyOpenGL>=3.1.7
