# Resources

Static assets available to *any* program based on this core: button icons,
default fonts, notification sounds, placeholder images, default styles.

Assets that belong to the C2UI editor specifically go to `App/Data` instead.
