# Fonts

Drop `.ttf` / `.otf` files here. They are registered with `QFontDatabase` at startup
(see `Core/Library/app.py`) and can then be referenced by family name in a theme's
`font.family` token.

Suggested (not bundled for licensing reasons): Inter, Roboto, JetBrains Mono.
